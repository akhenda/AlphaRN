import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  content: {
    flex: 1,
  },
  backgroundImage: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
